import { Button } from "@/components/ui/button";
import {
  Card,
  CardContent,
  CardDescription,
  CardHeader,
  CardTitle,
} from "@/components/ui/card";
import { FileVideo2, Zap, Clock, Download } from "lucide-react";
const page = () => {
  return <section className=" min-h-screen">page</section>;
};

export default page;
